# Stateful MySQL

- Stores MySQL database on the `db` folder.
- `.gitignore` includes the name of the `/db` to avoid committing.
- Source code is mapped to repo path, so you can compile and test using this image.